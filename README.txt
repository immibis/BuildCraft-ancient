This is BuildCraft (SVN version) ported to use the new Forge ID assignment system as an example and for testing.

Notes:
	Legacy pipe support was removed.

Currently unsolved bugs:
	TextureOilFX doesn't work - stationary oil has a plain black texture instead
